
export const authReducer =(state={},action)=>{
    switch (action.type) {
        case "LOGIN":
            
            return;
        case "LOGOUT":
            
            return
        default:
            break;
    }
}